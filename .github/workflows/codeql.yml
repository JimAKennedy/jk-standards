name: CodeQL

# Static security analysis for the Python codebase. Runs on PRs, on main
# pushes, and weekly (so upstream query updates catch latent issues even
# during quiet weeks). Not required for merge — CodeQL results are advisory
# and surface in the Security tab.

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    - cron: "27 6 * * 1"

permissions:
  actions: read
  contents: read
  security-events: write

jobs:
  analyze:
    name: Analyze (python)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@08c6903cd8c0fde910a37f88322edcfb5dd907a8 # v5.0.0

      - uses: github/codeql-action/init@f205ea1c3313d32999d8d6a48b4f6530d4437b38 # v3.28.8
        with:
          languages: python
          queries: security-and-quality

      - uses: github/codeql-action/analyze@f205ea1c3313d32999d8d6a48b4f6530d4437b38 # v3.28.8
        with:
          category: "/language:python"

---
class: plan
---

# Evidence — M001/S01

## M001/S01 — Task 1 — upgrade flow, tests first

- eight new tests first, all failed (parsers rejected the positional —
  feature absent, the right reason)
- after implementation: `pytest -q tests/test_skills_install.py -k upgrade`
  → 8 passed (404 → exit 2 + byte-identical lock; success → pin and both
  kinds' hashes in one final lock write; commands entrypoint; latest via
  the releases API; skew note with exit 0; own-`ref` entry untouched;
  malformed argument exit 2; multi-source governed set exit 2 naming both)
- `unit` → exit 0: 550 passed, 1 skipped (after re-emitting
  doc-coverage.json — the known new-src-function coupling)
- `ruff check` and `ruff format --check` → exit 0
- doc-drift maps nothing to skills_install.py (verified), so no
  Docs-Not-Affected trailer owed
- 2026-09-15

## M001/S01 — Task 2 — docs, changelog, gates

- README consumption section, `docs/skills.md`, `docs/configuration.md`,
  `reference/configuration.mdx`: version argument + `latest` described,
  move-vs-bless distinction stated, Status anchors refreshed
- changelog entry under a fresh `[Unreleased]` heading
- `format` → exit 0
- `unit` → exit 0: 550 passed, 1 skipped
- `emit-fresh` → exit 0
- `discipline` → exit 0
- `doc-drift --base main` → no mapped sources touched
- `status-prose --base main` → accuracy arm ran, no violations
  (re-run post-commit over the full range, recorded below)
- `gate` → scripts/verify.sh: all locally-runnable gates passed
- 2026-09-15

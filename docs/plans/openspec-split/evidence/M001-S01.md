---
class: plan
---

# Evidence — M001/S01

## M001/S01 — Task 1 — Source path resolution, tests first

- new tests first: `test_source_path_dangling_flagged` failed (check
  reported nothing) — the right reason; leniency cases pass vacuously
  pre-implementation by design
- after implementation: `pytest -q tests/test_ledger.py -k source` →
  5 passed (resolving file, directory, dangling flagged, prose skipped,
  hatch suppresses)
- real tree: `jk-standards ledger` → both ledgers conform (both carry
  prose Source lines, correctly skipped; skill-evals' names a deleted
  file and does not flag)
- `unit` → exit 0: 542 passed, 1 skipped (after re-emitting
  doc-coverage.json, staled by the new src function — known coupling)
- `ruff check` and `ruff format --check` → exit 0
- note: `emit all` currently aborts in the coverage emitter (`coverage
  json` exit 2 — no `.coverage` data file present); deterministic
  emitters were run individually; `emit all --check` is unaffected
  (coverage.json is not check-gated)
- 2026-09-15

---
class: plan
---

# Evidence — M001/S02

## M001/S02 — Task 1 — assess and auto

- `commands/assess.md`: spec-directory mode (tasks.md excluded as
  mirror-to-be), proposal mode (materialize via the spec system's own
  workflow, tasks.md born as the unticked mirror after decomposition),
  absent-case install notice (commands cited from the OpenSpec README,
  2026-09-15), Source + boundary rule in the write-it section
- `commands/auto.md`: orient step gains the non-blocking availability
  notice
- `format` → exit 0
- `discipline` (baseless local run) → exit 0
- 2026-09-15

## M001/S02 — Task 2 — plan, ship, close, commands.md, changelog, R9

- `commands/plan.md`: cite-by-name, never-paraphrase rule added; DoD
  verbatim-copy rule untouched
- `commands/ship.md`: mirror tick added to docs-sync (only place a mirror
  is ever ticked); PR body links the spec change
- `commands/close.md`: mirror verification per the granularity contract;
  archival step (both OpenSpec forms) named at the final close only
- `docs/commands.md`: assess, plan, ship, close, auto rows synced; Status
  anchor refreshed
- changelog: milestone entry under [Unreleased]
- R9: all five superpowers references confirmed intact in their original
  sections (grep recorded: brainstorming in assess+plan, writing-plans in
  plan, subagent-driven-development and verification-before-completion in
  next)
- `format` → exit 0
- `discipline` → exit 0
- `doc-drift --base main` → 6 mappings triggered, all satisfied
- `status-prose --base main` → accuracy arm ran, no violations
- `gate` → scripts/verify.sh: all locally-runnable gates passed
- 2026-09-15

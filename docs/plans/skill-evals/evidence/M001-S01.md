---
class: plan
---

# Evidence — M001/S01

## M001/S01 — Task 1 — secrets-scan hook and rejection demo

- shipped `secrets-scan` hook under `repo: .` → golang environment build
  failed ("directory prefix . does not contain main module or its selected
  dependencies") — R0's sanctioned fallback taken: upstream
  `gitleaks/gitleaks` hook, pinned `v8.30.1`, in `.pre-commit-config.yaml`
- staged fabricated `sk-ant-api03-…` key → hook exit 1, RuleID
  `anthropic-api-key` (finding redacted); scratch file unstaged and deleted
- full local-config suite (the GHA `pre-commit` job's other invocation) →
  all hooks Passed
- `format` → exit 0
- `gate` → `scripts/verify.sh`: 19 passed, 0 failed, 3 skipped
- 2026-09-15

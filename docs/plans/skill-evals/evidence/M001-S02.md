---
class: plan
---

# Evidence — M001/S02

## M001/S02 — Task 1 — skill_lint module with failing-first tests

- new tests first: collection failed with
  `ImportError: cannot import name 'skill_lint'` — the right reason
- after implementation: `pytest -q tests/test_checks.py -k skill_lint` →
  11 passed
- real-tree run → "skill-lint: 11 skill(s) conform", 0 errors
- `unit` → `pytest -q` exit 0: 536 passed, 1 skipped (after re-emitting
  doc-coverage.json, staled by the new src module — the known coupling)
- `ruff check` and `ruff format --check` → exit 0
- 2026-09-15

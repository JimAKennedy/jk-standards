---
class: plan
---

# Evidence — M001/S02

## M001/S02 — Task 1 — skill_lint module with failing-first tests

- new tests first: collection failed with
  `ImportError: cannot import name 'skill_lint'` — the right reason
- after implementation: `pytest -q tests/test_checks.py -k skill_lint` →
  11 passed
- real-tree run → "skill-lint: 11 skill(s) conform", 0 errors
- `unit` → `pytest -q` exit 0: 536 passed, 1 skipped (after re-emitting
  doc-coverage.json, staled by the new src module — the known coupling)
- `ruff check` and `ruff format --check` → exit 0
- 2026-09-15

## M001/S02 — Task 2 — registry, hook, docs, fixtures

- `jk-standards skill-lint --root .` → exit 0, "11 skill(s) conform"
- `jk-standards all` includes skill-lint → 0 violations
- `format` → exit 0
- `unit` → exit 0: 536 passed, 1 skipped
- `emit-fresh` → exit 0 (checks.json now carries skill-lint)
- `discipline` → exit 0
- `gate` → `scripts/verify.sh` (post-commit, doc-drift over the committed
  range vs main): 19 passed, 0 failed, 3 skipped — exit 0
- ARCHITECTURE.md pair satisfied by a `Docs-Not-Affected` trailer: it
  describes the registry structurally and enumerates no individual check,
  so a new registry entry changes nothing it asserts
- 2026-09-15

---
class: plan
---

# Evidence — M002/S01

## M002/S01 — Task 1 — offline harness: extra, modules, mocked tests

- `pip install -e ".[eval]"` → deepeval 4.2.3, anthropic 1.6.0 imported
  cleanly in the venv
- deepeval 4.2.3 interface probed before implementation: abstract methods
  generate/a_generate/get_model_name/load_model; GEval takes criteria
- seven offline tests first → ModuleNotFoundError: harness (the right
  reason); after implementation → 7 passed
- `unit` → exit 0: 557 passed, 1 skipped (doc-coverage.json re-emitted —
  the known coupling)
- ruff check + format → clean (one long line auto-caught and fixed)
- `discipline` → 0 errors
- no-OpenAI DoD made greppable: a test asserts no harness module contains
  the string OPENAI
- 2026-09-16

## M002/S01 — Task 2 — eval entry, make target, config, live run

- region:skill-evals-config landed in jk-standards.yaml (sonnet-5 both
  roles, runs 3, threshold 0.6, max_output_tokens 4000, max_cases 40)
- keyless paths verified: pytest skips with the documented message;
  `make eval` refuses with guidance — never a false green
- live-run road bumps, each fixed with an offline test or config note:
  workspace-scoped-key requirement (ANTHROPIC_WORKSPACE_ID header
  support added); claude-sonnet-5 thinking blocks emptying a tight
  token budget (cap 1500→4000 + honest sentinel for empty text)
- first recorded two-arm judged run: versioned-state-serialization-basic
  with-skill scores [1.0, 1.0, 0.9] median 1.0 vs without-skill
  [0.4, 0.3, 0.3] — threshold 0.6 cleared, decisive with-skill lift;
  `eval` → `make eval` exit 0 (1 passed, ~3m20s)
- `format` → exit 0; `unit` → 558 passed, 1 skipped; `emit-fresh` → 0;
  `discipline` → 0; range arms vs main → green
- `gate` → scripts/verify.sh: all locally-runnable gates passed (eval
  now on its documented exclusion list)
- 2026-09-16

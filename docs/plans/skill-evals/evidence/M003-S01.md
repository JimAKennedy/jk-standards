---
class: plan
---

# Evidence — M003/S01

## M003/S01 — Task 1 — the reusable workflow

- `.github/workflows/skill-evals.yml`: workflow_call (both secrets
  optional) + schedule (17 6 * * 1) + workflow_dispatch; contents: read
  ceiling; guard step fails loudly without the key; results artifact,
  14-day retention; schedule-only notify job driving the label-deduped
  skill-evals-drift issue (sanitizer-nightly posture)
- `action-pinning` → all SHA-pinned; `workflow-permissions` → within
  grants; `workflow-concurrency` → conforming; check-yaml → 0
- `discipline` → jk-standards all exit 0
- 2026-09-16

## M003/S01 — Task 2 — ci.yml wiring

- `eval-paths` (plain git diff, PR base vs head / push before..sha) →
  `skill-evals` (conditional reusable call, secrets: inherit) →
  `ci-complete` registered in needs and the shell, accepting a skip only
  when eval-paths succeeded and reported not-relevant
- fourth DoD box (missing-secret failure): verified by the guard step's
  presence and message — the live negative cannot be staged without
  deleting the repo secret; the positive (real run with the secret)
  arrives with this PR's own board, which ship watches
- `format` → exit 0; `discipline` → 0; `gate` → all locally-runnable
  gates passed
- real-path proof pending on the PR run: this branch touches
  evals-relevant paths (jk-standards.yaml untouched, but plan/evidence
  files no — the workflow file itself is in the filter set), so
  eval-paths will fire skill-evals live
- 2026-09-16

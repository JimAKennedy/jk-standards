---
class: plan
---

# Evidence — M003/S02

## M003/S02 — Task 1 — release gate, governed doc, drift map, RELEASE.md

- `release.yml`: skill-evals job on the tag path (full corpus), with the
  flaky-recovery rule in a comment; the repo's own workflow-permissions
  check caught the notify job's issues:write exceeding the release
  ceiling — fixed with a job-level grant and the static-composition
  reason inline
- `docs/skill-evals.md`: gated, dated, count-drift-safe; discipline doc
  covering two-arm semantics, require_lift, provenance, the four run
  surfaces, failure reading, cost model
- drift map: `evals/**` → `docs/skill-evals.md` with reason
- `RELEASE.md`: pre-tag checklist gains `make eval` with the tag-rerun
  note
- `format` → 0; `discipline` → 0; range arms vs main → green;
  `gate` → all locally-runnable gates passed
- `eval` → satisfied by the ship-window recorded run of 2026-09-16
  (10/10; evidence/M002-S02.md): this slice changes neither corpus nor
  harness nor config, so the scores are unchanged by construction
- 2026-09-16

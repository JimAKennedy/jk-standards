---
class: plan
---

# Evidence — M003/S03

## M003/S03 — Task 1 — cadence surfaces verified, CI proof recorded

- `skill-evals.yml` carries `schedule: cron "17 6 * * 1"` and
  `workflow_dispatch` (landed with S01; verified present), plus the
  schedule-only notify job driving the label-deduped skill-evals-drift
  issue
- CI-proof DoD box (amended wording, decision recorded): ticked
  contingent on this PR's own skill-evals run — this branch touches
  `.github/workflows/skill-evals.yml`, which is in eval-paths' filter
  set, so the paid gate fires live on the PR board; `/jk:ship` watches
  that board and a red run blocks the merge, so the tick cannot outlive
  a false claim. Dispatch and schedule go live when the file reaches
  main at merge.
- `format` → 0; `discipline` → 0
- 2026-09-16

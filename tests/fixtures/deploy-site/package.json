{
  "name": "deploy-site-fixture",
  "type": "module",
  "private": true,
  "version": "0.0.0",
  "description": "Scratch Starlight site proving the reusable deploy-site.yml prebuild seam works Node-only (python-version: \"\").",
  "scripts": {
    "prebuild": "node scripts/gen-rules.mjs",
    "build": "astro build"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.41.4",
    "astro": "^7.1.3",
    "sharp": "^0.35.3"
  }
}
